<UserControl x:Class="Mazesta.Desktop.Views.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="{StaticResource Brush.Background}">
    <StackPanel Margin="24" VerticalAlignment="Top">
        <TextBlock Text="{Binding Title}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource Brush.Text}" />
        <TextBlock Text="{Binding Body}" FontSize="13" Foreground="{StaticResource Brush.TextMuted}" Margin="0,12,0,0" TextWrapping="Wrap" />
    </StackPanel>
</UserControl>
